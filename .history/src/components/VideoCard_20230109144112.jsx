import { Link } from 'react-router-dom';
import { Typography, Card, CardContent, CardMedia} from '@mui/material';
import { CheckCircle } fro

const VideoCard = () => {
  return (
    <div>VideoCard</div>
  )
}

export default VideoCard